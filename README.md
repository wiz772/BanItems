# 🚫 BanItems – Plugin Minecraft

**BanItems** est un plugin Minecraft permettant de **bannir des items spécifiques** via un fichier de configuration.  
Dès qu’un item interdit est détecté dans l’inventaire d’un joueur, il est **automatiquement supprimé**.

---

## ✨ Fonctionnalités

- Bannissement d’items via `config.yml`
- Suppression automatique des items interdits
- Vérification en temps réel de l’inventaire
- Compatible survie / créatif
- Léger et optimisé

---

## 📥 Installation

1. Télécharge le fichier `BanItems.jar` depuis la section **Releases** du GitHub
2. Place le fichier `.jar` dans le dossier `plugins/` de ton serveur
3. Démarre ou redémarre ton serveur
4. Modifie le fichier `config.yml` pour bannir les items souhaités
5. Recharge le plugin avec `/banitems reload` ou redémarre le serveur

---

## 📜 Permissions

| Permission | Description |
|-----------|-------------|
| `banitems.reload` | Permet de recharger la config |
| `banitems.bypass` | Permet de bypass le plugin |

---